# =============================================================================
# HALLO Projects — Environment Variables for Docker Compose
# =============================================================================
# This file is used by docker-compose.yml (full stack)
# Values should be configured for production use

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------
DOMAIN=projects.example.com

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://hallo:hallo_docker_password@postgres:5432/hallo_projects
DB_PASSWORD=hallo_docker_password

# -----------------------------------------------------------------------------
# Cache & Queue (Redis)
# -----------------------------------------------------------------------------
REDIS_URL=redis://redis:6379

# -----------------------------------------------------------------------------
# API (apps/api)
# -----------------------------------------------------------------------------
PORT=4000

# JWT — generate with: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h

# Encryption key for provider credentials — must be exactly 32 bytes hex
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_32_byte_hex_encryption_key

# GitHub Webhook secret — generate with: openssl rand -hex 20
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret

# Log level: error | warn | info | debug | verbose
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Web (apps/web)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=https://projects.example.com/api

# -----------------------------------------------------------------------------
# Worker (apps/worker)
# -----------------------------------------------------------------------------
# Health check interval in seconds (default: 60)
HEALTH_CHECK_INTERVAL=60

# Timeout per health check request in seconds (default: 10)
HEALTH_CHECK_TIMEOUT=10

# Polling interval for deployment status in seconds (default: 5)
DEPLOYMENT_POLL_INTERVAL=5
